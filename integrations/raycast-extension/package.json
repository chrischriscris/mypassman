{
  "name": "mypassman",
  "title": "MyPassman",
  "description": "Search your mypassman vault and fill secrets — fields never touch JS, the CLI does the filling",
  "icon": "icon.png",
  "author": "chus",
  "license": "MIT",
  "preferences": [
    {
      "name": "binaryPath",
      "title": "mypassman Binary Path",
      "description": "Path to the mypassman binary (~ is expanded)",
      "type": "textfield",
      "required": false,
      "default": "~/.local/bin/mypassman",
      "placeholder": "~/.local/bin/mypassman"
    },
    {
      "name": "siteIcons",
      "title": "Site Icons",
      "label": "Show favicons for items with a URL",
      "description": "Fetches each item's domain icon from a third-party favicon service (api.ray.so). Disable to keep vault domains fully local.",
      "type": "checkbox",
      "required": false,
      "default": true
    }
  ],
  "commands": [
    {
      "name": "list-items",
      "title": "Search Vault",
      "description": "Search vault items; Enter fills into the frontmost app, ⌘K copies via concealed clipboard",
      "mode": "view"
    }
  ],
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint"
  },
  "dependencies": {
    "@raycast/api": "^1.83.0",
    "@raycast/utils": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "@types/react": "^19.0.10",
    "react": "^19.3.0",
    "typescript": "^5.9.3"
  }
}
