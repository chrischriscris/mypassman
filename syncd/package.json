{
  "name": "mypassman-syncd",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "node --test test/manifest-fixtures.test.mjs && node test/run-contract.mjs worker",
    "test:rust": "node test/run-contract.mjs rust"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.135.0"
  }
}
